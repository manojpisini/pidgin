[package]
name = "pidgin-cli"
version = "0.1.0"
edition = "2024"

[dependencies]
clap = { version = "4.6.1", default-features = false, features = ["std", "derive"] }
pidgin-core = { path = "../pidgin-core" }
